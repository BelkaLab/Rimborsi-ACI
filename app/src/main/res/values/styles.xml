<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="cpbStyle">@style/CircularThemeProgressBar</item>
        <item name="android:editTextStyle">@style/MaterialEditText</item>
        <item name="editTextStyle">@style/MaterialEditText</item> <!-- For compatibility with the support library -->
    </style>

    <style name="CircularThemeProgressBar">
        <item name="cpb_color">@color/colorAccent</item>
    </style>

    <style name="MaterialEditText" parent="Widget.AppCompat.EditText">
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Toolbar" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>
    </style>
</resources>
